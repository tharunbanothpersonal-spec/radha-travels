<header class="site-header">
  <div class="container header-wrap">
    <a href="/" class="brand">
      <img src="/images/logo.png" alt="Radha Travels" class="brand-logo" />
      <div class="brand-name"><div class="premium-plate">
    <span class="brand-r">RADHA</span>
    <span class="brand-divider"></span>
    <span class="brand-t">TRAVELS</span>
  </div>
  </div>
    </a>
<a href="/" class="brand-mobile">
  <div class="premium-plate">
    <span class="brand-r">RADHA</span>
    <span class="brand-divider"></span>
    <span class="brand-t">TRAVELS</span>
  </div>
</a>
    <nav class="nav" id="siteNav" aria-label="Primary">
      <a href="/">Home</a>
      <a href="/services">Services</a>
      <a href="/blog">Travel Guide</a>
      <a href="/fleet">Fleet</a>
      <a href="/gallery">Gallery</a>
      <a href="/about">About Us</a>
      <a href="/happy-customers">Reviews</a>
      <!-- ADMIN LOGIN BUTTON: paste into views/partials/header.ejs (or your header area) -->

  <a href="/admin/login" class="rt-admin-nav-btn" data-tooltip="Admin Login">
  <svg class="rt-user-svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path>
    <circle cx="12" cy="7" r="4"></circle>
  </svg>
  <span>
    Admin
  </span>
</a>
    </nav>
    <a href="#" role="button" class="btn-header open-booking">Book Now</a>



    <!-- Hamburger -->
    <button
      class="burger"
      id="navToggle"
      aria-label="Open menu"
      aria-controls="siteNav"
      aria-expanded="false"
      type="button">
      <span></span><span></span><span></span>
    </button>
  </div>
</header>

<%- include('social-dock') %> <!-- global social dock -->

<script>
  (function () {
    function initNav() {
      const burger = document.getElementById('navToggle');
      const nav    = document.getElementById('siteNav');
      if (!burger || !nav) return;

      const openNav  = () => {
        nav.classList.add('open');
        burger.classList.add('is-open');
        burger.setAttribute('aria-expanded', 'true');
        burger.setAttribute('aria-label', 'Close menu');
        document.body.classList.add('nav-open');
      };

      const closeNav = () => {
        nav.classList.remove('open');
        burger.classList.remove('is-open');
        burger.setAttribute('aria-expanded', 'false');
        burger.setAttribute('aria-label', 'Open menu');
        document.body.classList.remove('nav-open');
      };

      const toggle = () => (nav.classList.contains('open') ? closeNav() : openNav());

      // reset previous listeners
      burger.onclick = null;
      burger.addEventListener('click', toggle);

      // close when a nav link is clicked (mobile)
      nav.querySelectorAll('a').forEach(a => a.addEventListener('click', closeNav));

      // close on ESC
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && nav.classList.contains('open')) closeNav();
      });
    }

    if (document.readyState !== 'loading') initNav();
    else document.addEventListener('DOMContentLoaded', initNav);
  })();
</script>
